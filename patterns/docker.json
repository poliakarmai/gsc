[
  {"echelon": 2, "category": "CRITICAL", "title": "Docker: latest tag in production", "pattern_type": "regex", "search_pattern": "FROM\\s+\\S+:latest", "description": ":latest tag is non-deterministic — pin to specific version"},
  {"echelon": 2, "category": "HIGH", "title": "Docker: Running as root", "pattern_type": "semantic", "search_pattern": "^(FROM|USER)(?!.*USER\\s+(?!root))", "description": "Container runs as root — add USER directive"},
  {"echelon": 2, "category": "HIGH", "title": "Docker: Secret in ENV", "pattern_type": "regex", "search_pattern": "ENV\\s+(PASSWORD|SECRET|TOKEN|KEY)\\s*=", "description": "Secrets in Dockerfile layers — use build args or secrets mount"},
  {"echelon": 2, "category": "MEDIUM", "title": "Docker: apt-get without cleanup", "pattern_type": "regex", "search_pattern": "apt-get install(?!.*rm -rf /var/lib/apt)", "description": "Add rm -rf /var/lib/apt/lists/* to reduce image size"},
  {"echelon": 2, "category": "MEDIUM", "title": "Docker: EXPOSE all interfaces", "pattern_type": "regex", "search_pattern": "EXPOSE\\s+\\d+(?!.*127\\.0\\.0\\.1)", "description": "Port exposed on 0.0.0.0 — bind to 127.0.0.1 if only local access needed"},
  {"echelon": 1, "category": "LOW", "title": "Docker: Multiple RUN layers", "pattern_type": "semantic", "search_pattern": "RUN.*\\nRUN", "description": "Chain RUN commands with && to reduce layers"},
  {"echelon": 2, "category": "MEDIUM", "title": "Docker: HEALTHCHECK missing", "pattern_type": "semantic", "search_pattern": "^(?!.*HEALTHCHECK)", "description": "No HEALTHCHECK — container may appear healthy when broken"},
  {"echelon": 2, "category": "HIGH", "title": "Docker: privileged mode", "pattern_type": "regex", "search_pattern": "--privileged|privileged:\\s*true", "description": "Privileged container has full host access — use cap_add instead"}
]
