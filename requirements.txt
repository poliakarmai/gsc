# DD-04: pinned from pyproject.toml — single source of truth.
# Re-sync when pyproject.toml version ranges change (fastapi/pydantic/etc).
# Core (pyproject [project].dependencies)
PyYAML==6.0.3
requests==2.33.0
# Cloud (pyproject [project.optional-dependencies].cloud)
fastapi==0.141.1
uvicorn[standard]==0.51.0
pydantic==2.13.4
httpx==0.28.1
python-jose[cryptography]==3.5.0
stripe==15.4.0
